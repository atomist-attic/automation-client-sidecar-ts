subscription UpdateOnPush {
    Push(branch: $branch) {
        branch
        repo(owner: $owner, name: $repository) @required {
            name
            owner
            channels {
                name
                team {
                    id
                }
            }
        }
        after {
            sha
        }
    }
}
