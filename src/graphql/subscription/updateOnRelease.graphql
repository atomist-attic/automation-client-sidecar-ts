subscription UpdateOnRelease {
    Release {
        tag {
            commit(owner: $owner, name: $repository) @required {
                sha
                repo {
                    name
                    owner
                }
                pushes {
                    branch(name: $branch) @required {
                        name
                    }
                }
            }
        }
    }
}
